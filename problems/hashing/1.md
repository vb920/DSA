# 1. Two Sum

🔁 Iteration 1

**Pattern:** Hashing

**Difficulty:** Easy

**Time:** 5m

**Solo:** Y optimal

### 🧠 Core Logic

*   **The Trick:** map **value → index**; check complement **before** insert
*   **Execution:**
    1.  for each `i`, let `compl = target - nums[i]`
    2.  if `compl` in map → return the two indices
    3.  else put `nums[i] → i` and continue
*   **Complexity:**

Time `O(N)`

Space `O(N)`

### 🚨 Traps & Bugs

*   **Edge Case:** duplicates/self-pairing avoided by check-before-insert; typical constraints prevent overflow
*   **My Bugs:** none; map ops and array bounds handled cleanly

### 🎤 1-Sentence Pitch

*   “Single-pass hash map turns the `O(N²)` double loop into `O(N)` by instant complement lookup.”
