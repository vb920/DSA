# 217. Contains Duplicate

🔁 Iteration 1

**Pattern:** Hashing

**Difficulty:** Easy

**Time:** 3m

**Solo:** Y optimal

### 🧠 Core Logic

*   **The Trick:** keep a **set of seen values**; return on the **first repeat**
*   **Execution:**
    1.  for each `x` in `nums`
    2.  if `x` in set → return `true`
    3.  else add `x` to set and continue
*   **Complexity:**

Time `O(N)`

Space `O(N)`

### 🚨 Traps & Bugs

*   **Edge Case:** empty/size-1 arrays → `false`; negatives/large values fine; early-exit keeps it fast
*   **My Bugs:** none; check-before-insert and loop bounds handled cleanly

### 🎤 1-Sentence Pitch

*   “Single-pass hash set flags duplicates in `O(N)` via constant-time membership checks.”
