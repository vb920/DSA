# 739. Daily Temperatures

🔁 Iteration 1

**Pattern:** Monotonic Stack

**Difficulty:** Medium

**Time:** 15m

**Solo:** Y optimal

### 🧠 Core Logic

*   **The Trick:** maintain a **monotonic decreasing stack** of (temperature, index) while scanning from **right → left**; after popping all `≤` temps, the top gives the **next strictly warmer** day for the current index.
*   **Execution:**
    1.  initialize an empty stack (store **indices** or `(temp,index)` pairs).
    2.  iterate `i = n-1 … 0`:
        *   **pop** while stack top’s temperature `≤ temperatures[i]` (we need **strictly greater**).
        *   if stack non-empty, distance = `topIndex - i`, else distance = `0`.
        *   **push** current day onto stack.
    3.  return the distances array.
*   **Complexity:**

Time `O(N)`

Space `O(N)`

### 🚨 Traps & Bugs

*   **Strictness:** must find **next strictly warmer** → pop while `≤` (not `<`).
*   **Data structure:** using a **stack of indices** is simpler and avoids storing pairs; prefer `ArrayDeque<Integer>` over legacy `Stack`.
*   **Write-in-place:** you can fill `ans[i]` directly during the right-to-left pass (no need to build a list and `reverse()`).
*   **Edge Cases:** last day always `0`; stretches of equal temps should **pop** until a strictly warmer day is found or stack empties.

### 🎤 1-Sentence Pitch

*   “Scan right-to-left with a **decreasing stack** so each day jumps to its next **strictly warmer** day in amortized `O(1)`, giving an overall `O(N)` solution.”
