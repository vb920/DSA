# 682. Baseball Game

🔁 Iteration 1

**Pattern:** Stack

**Difficulty:** Easy

**Time:** 10m

**Solo:** Y optimal

### 🧠 Core Logic

*   **The Trick:** simulate the scoring rules with a **stack of round scores**—`"C"` removes last, `"D"` doubles last, `"+"` sums last two—so future rounds can depend on prior results.
*   **Execution:**
    1.  initialize an empty stack
    2.  for each token in `operations`:
        1.  if it’s `"C"` → `pop()`
        2.  else if `"D"` → `push(2 * top)`
        3.  else if `"+"` → pop top `a`, peek `b`, `push(a)` back, then `push(a + b)`
        4.  else (a number, possibly negative) → `push(parseInt(token))`
    3.  sum all values in the stack and return
*   **Complexity:**

Time `O(N)`

Space `O(N)`

### 🚨 Traps & Bugs

*   **Edge Case:** tokens can be **negative integers** (handled since you compare exact operator strings before parsing).
*   **Data structure & efficiency:** using `Stack<String>` causes repeated `parseInt` conversions; prefer `ArrayDeque<Integer>` and keep integers throughout; you can also keep a **running sum** (update on each op) to avoid the final summation pass.
*   **Operator assumptions:** `"D"` and `"+"` require at least 1 or 2 prior scores—LeetCode guarantees valid sequences.
*   **My Bugs:** none; logic/order for `"+"` (pop one, peek next, restore, then push sum) is correct.
