# 20. Valid Parentheses

🔁 Iteration 1

**Pattern:** Stack

**Difficulty:** Easy

**Time:** 5m

**Solo:** Y optimal

### 🧠 Core Logic

*   **The Trick:** use a **stack of opening brackets** and, on each closing bracket, **check the top matches its pair**; stack must be **empty at the end**
*   **Execution:**
    1.  for each `ch` in `s`:
    2.  if `ch` is an opening bracket `(` `[` `{` → **push** onto stack
    3.  else (closing): if stack empty → `false`; **pop** and verify it’s the matching opener
    4.  after the loop, return `stack.isEmpty()`
*   **Complexity:**

Time `O(N)`

Space `O(N)` (worst‑case all openers)

### 🚨 Traps & Bugs

*   **Edge Case:** empty string → `true`; **odd length** → can early‑return `false`; characters are guaranteed to be only brackets in this problem
*   **Implementation nit:** prefer `ArrayDeque<Character>` over legacy `Stack` for stack behavior; also a small map `{')':'(', ']':'[', '}':'{'}` makes matching clearer and less error‑prone than chained conditions
*   **My Bugs:** none; logic correct—handled empty‑stack on close and validated matching pairs

### 🎤 1-Sentence Pitch

*   “Scan once, pushing openers and verifying each closer against the stack top—valid iff all matches succeed and the stack ends empty.”
